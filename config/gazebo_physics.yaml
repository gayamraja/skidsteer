# Gazebo Physics Configuration
# Friction and physics parameters for accurate simulation
# Updated for dual-chain drive with jackshaft

gazebo_physics:
  # Physics Engine Settings
  physics:
    type: "ode"              # Physics engine: ode or bullet
    max_step_size: 0.001     # Maximum time step (1ms for 1000Hz)
    real_time_factor: 1.0    # Real-time factor (1.0 = real-time)
    real_time_update_rate: 1000.0  # Update rate in Hz
    
  # Ground Material Properties
  ground:
    mu1: 1.0                 # Longitudinal friction (high for climbing)
    mu2: 0.5                 # Lateral friction (lower for skid-steer)
    kp: 1000000.0            # Contact stiffness
    kd: 10.0                 # Contact damping
    
  # Dual-Chain Drive Characteristics
  # Two-stage chain: 16T→36T (2.25:1) then 16T→16T (1:1)
  chain_drive:
    total_reduction: 2.25    # Chain reduction only (16T→36T)
    system_reduction: 22.5   # Total with 10:1 gearbox
    backlash: 0.02           # Increased backlash from two chains
    damping_coefficient: 75.0  # Higher damping from dual-chain system
    jackshaft_inertia: 0.02  # Rotational inertia of jackshaft + 36T sprocket
    
  # Note: Wheel friction (mu1=1.1, mu2=0.5) is set in URDF
  # Jackshaft damping (75.0) and wheel damping (25.0) simulate dual-chain system
